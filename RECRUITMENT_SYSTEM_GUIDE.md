# 온톨로지 기반 인사 채용 시스템 가이드

## 🎯 시스템 개요

이 시스템은 **AI/ML 없이 규칙 기반 추론(Rule-based Reasoning)**으로 구현한 지능형 채용 매칭 시스템입니다.

### 핵심 개념

**온톨로지(Ontology)**란?
- 지식을 표현하는 체계적인 방법
- 개념(Concept), 관계(Relation), 속성(Property)을 정의
- 예: "Java와 Kotlin은 유사한 기술이다" → 온톨로지 관계

**규칙 기반 추론**이란?
- 미리 정의된 규칙으로 결론을 도출
- AI 학습 없이도 지능적인 판단 가능
- 투명하고 설명 가능한 알고리즘

## 📊 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                   온톨로지 레이어                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Skill   │  │Candidate │  │JobPosting│              │
│  │(기술)    │  │(지원자)  │  │(공고)    │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│       │             │              │                     │
│       └─────────────┴──────────────┘                     │
│              온톨로지 관계 정의                           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│                   추론 엔진 레이어                         │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │SkillMatching     │  │CandidateMatching │            │
│  │Engine            │  │Service           │            │
│  │(기술 매칭)       │  │(종합 매칭)       │            │
│  └──────────────────┘  └──────────────────┘            │
│                                                          │
│  ┌──────────────────────────────────────┐              │
│  │ RecommendationEngine                 │              │
│  │ (추천 시스템)                        │              │
│  └──────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│                 애플리케이션 레이어                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │Recruitment│  │Candidate │  │Application│             │
│  │Service   │  │Service   │  │Service    │             │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

## 🧠 매칭 알고리즘

### 1. 기술 매칭 (Skill Matching)

```
입력: 지원자 기술, 요구사항 기술

1단계: 기술 유사도 계산
  - 정확 매칭: Java == Java → 1.0
  - 온톨로지 관계: Java ≈ Kotlin → 0.8
  - 같은 카테고리: Java ≈ Python → 0.5
  - 관련 없음: Java ≈ React → 0.0

2단계: 숙련도 매칭
  - 지원자 >= 요구사항 → 1.0
  - 지원자 < 요구사항 → 비율 계산

3단계: 경력 매칭
  - 지원자 >= 요구사항 → 1.0
  - 지원자 < 요구사항 → 비율 계산

최종 점수 = (유사도 × 0.5) + (숙련도 × 0.3) + (경력 × 0.2)
```

### 2. 종합 매칭 (Candidate Matching)

```
입력: 지원자, 채용 공고

1. 필수 기술 점수 (40%)
   - 모든 필수 기술에 대해 최고 매칭 점수 계산
   - 평균 점수 산출

2. 우대 기술 점수 (20%)
   - 모든 우대 기술에 대해 최고 매칭 점수 계산
   - 평균 점수 산출

3. 경력 점수 (25%)
   - 요구 경력 충족 시: 100점
   - 초과 경력: 보너스 (최대 120점)
   - 부족 경력: 비율 계산

4. 학력 점수 (15%)
   - 학사 이상: 100점
   - 그 외: 레벨 × 25점

최종 매칭 스코어 = 
  (필수기술 × 0.40) + 
  (우대기술 × 0.20) + 
  (경력 × 0.25) + 
  (학력 × 0.15)
```

## 📈 실제 매칭 예시

### 예시 1: 김개발 → 백엔드 공고

**공고 요구사항:**
- 필수: Java (ADVANCED, 3년), Spring Boot (ADVANCED, 3년), MySQL (INTERMEDIATE, 2년)
- 우대: AWS (INTERMEDIATE, 1년), MSA (INTERMEDIATE, 1년)

**지원자 프로필:**
- Java (EXPERT, 5년)
- Spring Boot (EXPERT, 4년)
- MySQL (ADVANCED, 5년)
- AWS (INTERMEDIATE, 3년)
- MSA (ADVANCED, 3년)
- 총 경력: 6년
- 학력: 학사

**매칭 계산:**
1. 필수 기술: 100점 (모든 요구사항 초과 충족)
2. 우대 기술: 100점 (모든 우대사항 충족)
3. 경력: 120점 (6년 > 3년, 보너스 적용)
4. 학력: 100점 (학사)

**최종 점수: 105.0점** ✨

### 예시 2: 박풀스택 → 백엔드 공고

**지원자 프로필:**
- Python (EXPERT, 7년) ← Java 요구사항과 다름
- Django (EXPERT, 7년) ← Spring Boot와 유사
- JavaScript (ADVANCED, 5년)
- React (ADVANCED, 4년)
- Docker (EXPERT, 5년)
- Kubernetes (ADVANCED, 3년)
- 총 경력: 8년

**매칭 계산:**
1. 필수 기술: 65점 (Python은 Java와 유사도 낮음, Django는 Spring과 유사)
2. 우대 기술: 80점 (클라우드 경험 있음)
3. 경력: 120점 (8년 > 3년, 보너스 적용)
4. 학력: 100점 (석사)

**최종 점수: 81.7점**

## 🔍 추천 시스템

### 1. 공고별 지원자 추천

```
알고리즘:
1. 모든 지원자에 대해 매칭 스코어 계산
2. 50점 이상만 필터링
3. 점수 내림차순 정렬
4. Top N 반환
```

### 2. 지원자별 공고 추천

```
알고리즘:
1. 모든 활성 공고에 대해 매칭 스코어 계산
2. 50점 이상만 필터링
3. 점수 내림차순 정렬
4. Top N 반환
```

### 3. 유사 지원자 찾기

```
알고리즘: Jaccard 유사도
1. 대상 지원자의 기술 집합 추출
2. 다른 지원자들의 기술 집합과 비교
3. 유사도 = |교집합| / |합집합|
4. 0.3 이상만 필터링
5. 유사도 내림차순 정렬
```

## 🚀 사용 방법

### 1. 시스템 시작

```bash
# 빌드
./gradlew clean build

# 실행
./gradlew bootRun
```

### 2. 웹 대시보드 접속

```
http://localhost:8080/recruitment/dashboard
```

### 3. API 테스트

```powershell
# PowerShell
powershell -ExecutionPolicy Bypass -File test-recruitment-simple.ps1

# 또는 Git Bash
bash test-recruitment-api.sh
```

## 📚 주요 API

### 기술 관리

```bash
# 기술 생성
POST /api/recruitment/skills
{
  "name": "Java",
  "category": "PROGRAMMING_LANGUAGE",
  "description": "객체지향 프로그래밍 언어"
}

# 유사 기술 관계 추가 (온톨로지)
POST /api/recruitment/skills/1/similar/2

# 기술 조회
GET /api/recruitment/skills
GET /api/recruitment/skills/1/similar
```

### 채용 공고

```bash
# 공고 생성
POST /api/recruitment/jobs
{
  "title": "백엔드 개발자",
  "description": "MSA 기반 시스템 개발",
  "departmentId": 1,
  "position": "SENIOR",
  "headcount": 2,
  "startDate": "2025-11-16",
  "endDate": "2026-01-16"
}

# 요구사항 추가
POST /api/recruitment/jobs/1/requirements?skillId=1&type=REQUIRED&minProficiency=ADVANCED&minYearsOfExperience=3

# 공고 공개
POST /api/recruitment/jobs/1/publish
```

### 지원자 관리

```bash
# 지원자 생성
POST /api/recruitment/candidates
{
  "name": "김개발",
  "email": "kim@example.com",
  "phone": "010-1234-5678",
  "education": "BACHELOR",
  "university": "서울대학교",
  "major": "컴퓨터공학"
}

# 기술 추가
POST /api/recruitment/candidates/1/skills?skillId=1&proficiencyLevel=EXPERT&yearsOfExperience=5

# 경력 추가
POST /api/recruitment/candidates/1/experiences?company=네이버&position=백엔드개발자&startDate=2019-03-01&endDate=2022-12-31
```

### 지원 및 매칭

```bash
# 지원 (자동 매칭 스코어 계산)
POST /api/recruitment/applications
{
  "candidateId": 1,
  "jobPostingId": 1,
  "coverLetter": "열정적으로 일하겠습니다!"
}

# 상세 매칭 분석
GET /api/recruitment/applications/1/matching-detail

# 공고별 지원자 조회 (매칭 스코어 순)
GET /api/recruitment/applications/job/1
```

### 추천 시스템

```bash
# 공고에 적합한 지원자 추천
GET /api/recruitment/recommendations/job/1/candidates?topN=10

# 지원자에게 적합한 공고 추천
GET /api/recruitment/recommendations/candidate/1/jobs?topN=10

# 유사 지원자 찾기
GET /api/recruitment/recommendations/candidate/1/similar?topN=5
```

## 💡 확장 가능성

### 1. 온톨로지 확장

```java
// 새로운 기술 추가
Skill rust = skillService.createSkill("Rust", SkillCategory.PROGRAMMING_LANGUAGE, "시스템 프로그래밍 언어");

// 유사 관계 추가
skillService.addSimilarSkill(rust.getId(), cpp.getId());
```

### 2. 매칭 알고리즘 조정

```java
// 가중치 변경
private static final double REQUIRED_SKILL_WEIGHT = 0.50;  // 필수 기술 비중 증가
private static final double EXPERIENCE_WEIGHT = 0.30;      // 경력 비중 증가
```

### 3. 새로운 추론 규칙 추가

```java
// 예: 특정 기술 조합에 보너스
if (hasSkill("Java") && hasSkill("Spring") && hasSkill("MSA")) {
    score += 10.0;  // 풀스택 보너스
}
```

## 🎓 학습 포인트

이 시스템을 통해 배울 수 있는 것:

1. **온톨로지 기반 지식 표현**
   - 도메인 개념 모델링
   - 관계 정의 및 활용

2. **규칙 기반 추론**
   - AI 없이 지능형 시스템 구현
   - 투명하고 설명 가능한 알고리즘

3. **DDD (Domain-Driven Design)**
   - 도메인 모델 중심 설계
   - 레이어 분리 및 책임 분산

4. **추천 시스템 기초**
   - 유사도 계산
   - 필터링 및 랭킹

5. **실무 적용**
   - 채용 플랫폼 구현
   - 자동화된 스크리닝

## 📊 성능 특성

- **매칭 속도**: O(N×M) - N: 지원자 수, M: 요구사항 수
- **추천 속도**: O(N×log N) - 정렬 포함
- **확장성**: 수천 명 규모까지 실시간 처리 가능
- **정확도**: 규칙 기반이므로 일관된 결과 보장

## 🔧 트러블슈팅

### 문제: 매칭 스코어가 너무 낮음
**해결**: 온톨로지 관계 추가 또는 가중치 조정

### 문제: 추천 결과가 없음
**해결**: 최소 점수 기준(50점) 낮추기

### 문제: 유사 지원자를 찾을 수 없음
**해결**: Jaccard 유사도 임계값(0.3) 낮추기
